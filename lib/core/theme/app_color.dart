import 'package:flutter/material.dart';

class AppColor {
  static const Color primary = Color(0xFFE09A1E);
  static const Color primaryColor = Color(0xFFE09A1E);
  static const Color gold = Color(0xFFFFD700);

  // Dark Theme Colors
  static const Color backgroundGradientStart = Color(0xFF000000); // Black
  static const Color backgroundGradientMiddle = Color(0xFF121212); // Dark Grey
  static const Color backgroundGradientEnd = Color(
    0xFF1E1E1E,
  ); // Slightly Lighter Grey
}
